import styled from "styled-components";

const Icono = styled.img`
  width: 34px;
  height: auto;
  margin: auto 10px;
  position: relative;
  cursor: pointer;
  z-index: 7;
`;

export default Icono;
